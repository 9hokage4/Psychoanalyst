# core/processor.py
import pandas as pd


def process_data(df: pd.DataFrame) -> pd.DataFrame:
    """
    # ! ЗАГЛУШКА 
    Эта функция должна:
    - Принять исходный DataFrame (результаты теста)
    - Выполнить агрегацию/анализ по заданному алгоритму
    - Вернуть новый DataFrame с результатами
    
    Пример входа:
        ФИО | Вопрос 1 | Вопрос 2 | ...
        Иван| 5        | 3        | ...
    
    Пример выхода (после обработки):
        Группа | Средний балл | Уровень тревожности
        Группа1| 4.2          | Высокий
    
    # * Текущая реализация - просто копия входа (для демонстрации)
    """
    # ! ВСТАВЬ СВОЙ АЛГОРИТМ ЗДЕСЬ
    return df.copy()  # временная заглушка